const separators = {
  letter: '',
  word: ' '
}

const split = (node, {
  // default options
  a11y = false,
  delimeter = separators.letter,
  element = 'span',
  each = () => {}
} = {}) => {

}

export default split
